{
	"folders": [
		{
			"name": "S-ECU",
			"path": "."
		},
		{
			"name": "Smart_ECU (firmware)",
			"path": "Firmware/Smart_ECU"
		}
	],
	"settings": {
		// "S-ECU" (repo root) already contains Firmware/Smart_ECU, which is
		// also listed above as its own root. Hide the nested copy so the
		// firmware shows once. The firmware root has no "Firmware" child, so
		// this only affects the repo-root view.
		"files.exclude": {
			"Firmware": true
		}
	}
}